[pytest]
testpaths = test
pythonpath = src
addopts = -ra
markers =
    unit: isolated tests
    integration: multi-module tests
    e2e: end-to-end workflow tests
    performance: performance tests
    failure_mode: failure-path tests
    path_resolution: relative path and cwd behavior tests
    recovery: retry/fallback/recovery tests
    slow: slower tests